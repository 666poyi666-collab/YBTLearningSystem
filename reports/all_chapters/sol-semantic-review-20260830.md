# gpt-5.6-sol 分章语义审计

## 范围与边界

- 复核范围：第一章第 2 节以后、第二章全部、第三至第五章全部，共 37 节、230 个学习循环、1,171 个教材项目。
- 第一章第 1 节沿用既有精修与实测证据，本轮未重复模拟。
- 本轮是课程、循环、教材项目和必刷题候选的语义审计，不是 1,209 项逐题作答模拟，也不是用户掌握验收。
- 全书机器结构仍为 5 章、38 节、1,209 项；完整结果见 `course-first-contract-audit.json`。

## 已修复

- 删除“把 section 课程列表平均切给 cycles”的生成方式；课程只在高置信目标例题/题型首次引入。
- 第二章 22 门课程重新定位，修复点线距离、线圆、圆圆、对称与圆方程的确定性错绑。
- 类型题优先使用教材显式类型标题；直属变式继承母题题型，不再全部堆入最后一个知识点循环。
- 第一章后续三节按课程编号重排，并修正 C15、C9、C12、C8 等桥接目标。
- 课程排序加入 `.a/.b`、`上/中/下`、`基础/提高/进阶` 的显式顺序。
- 必刷题保持选做且不阻塞主线；页 48、78-80 的跨节综合内容不再广播到单一节次全部 cycles。
- 恢复必刷题第 78 页可见但 OCR 漏掉的第 5 题身份，删除第 80 页由公式续行造成的伪重复题。

## 当前结论

- 26 节已通过当前课程映射合同。
- 12 节仍为 `needs_iteration`，原因不是题包缺失，而是存在尚未获得逐题/全文证据的 `unplaced_course_keys`。
- 这些课程仍保留在节次课程账本中，但不会进入完成门禁，也不会被硬绑定到近似题型。
- 第三至第五章已有若干历史视觉侧车缺口；它们继续保持 `UNVERIFIED`，不影响第一、二章当前学习主线。

## 待证据项

具体节次和课程列表以 `course-first-contract-audit.json` 的 `unplaced_course_keys` 为准。主要集中在：

- 第三章通用交点代数课程与椭圆/双曲线/抛物线具体项目的逐题对应；
- 4.8 数列拔高中的待定系数、递推、辅助数列与奇偶项项目；
- 5.1-5.5 导数综合课中的隐函数、原函数速解、含参二次先修和零点/不等式专题。

在原页或课程全文不足以确认前，系统必须对用户显示“不确定及缺少的证据”，不得静默猜测。
