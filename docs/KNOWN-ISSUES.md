# Known Issues

- 远程 Cloudflare MCP 已部署并导入第一、二章内容：Worker `math-learning-mcp` 当前版本为 `98671c0a-75c3-4ff9-95e9-280730567d3b`，内容快照 `v1-946594dec80d0b04`；D1 已有 2 章、11 节、401 个项目、37 门课程、1,365 条映射、2,038 个转写片段，R2 已有题包、转写包和 72 张题图。ChatGPT 已创建“数学一本通学习”开发模式 App 并发现 OAuth 端点，但授权页仍等待一次性验证码；不能用 Worker 健康检查替代真实令牌读取工具验收。
- 第二章 22 门课程已有完整文稿，但缺少可验证的逐句时间轴；恢复原视频/音频前不得伪造回看时间。
- 现有真实学习进度与内部模拟进度必须继续分离。
- Cloudflare D1/R2 导入已由 `cloud/mcp/scripts/import_content.mjs` 生成版本、哈希和可重复构建证据；后续内容变更必须生成新的版本号，不覆盖已有学习事件。
- 全量 Python 回归共 140 项：6 项既有失败、2 项既有错误、1 项因本机缺少 DeepSeek 配置跳过。问题集中在第一章 OCR 标签闭合、历史首图证据缺失、零基础模拟 source hash 过期和奖励链证据；本轮 MCP 文件没有修改这些 Python/数据路径，仍需单独恢复或重建对应证据。
