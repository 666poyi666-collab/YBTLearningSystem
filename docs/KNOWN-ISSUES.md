# Known Issues

- 远程 Cloudflare MCP 已运行内容版本 `v1-82aac1d4dce375de` 和 Worker `0f7f29b2-01e3-4d36-8f43-b375c329a939`。当前 546 条答案中只有 473 条允许自动使用；51 条 OCR 文本与 22 条仅原页证据必须返回原页核对，不能为了“自动化完整”放宽门禁。
- 第二章 22 门课程已有完整文稿，但缺少可验证的逐句时间轴；恢复原视频/音频前不得伪造回看时间。
- 本地静态资料与路线覆盖选择性必修 1 第一至第五章；38/38 题包和学习题包均为 `VERIFIED`。五人格运行只完成路线可执行性审计，没有最终数学作答，保持 `mathematical_assessment_status=not_run`；真人学习进度仍只随 MCP 中的真实事件推进。
- 全书课程语义审计当前为 38/38 节通过、0 个 `unplaced_course_keys`。最后 12 节的 39 个课程落点均记录目标循环、理由和转写 SHA-256；该状态只证明路线映射，不代表真人听完或掌握。
- 原书答案证据为 546/546，生产 `math_get_answer_sources` 已可按当前版本返回；默认只返回同一来源类型的最新版本，`includeHistory=true` 才查看旧版本，避免旧 50 条记录形成重复当前答案。
- 现有真实学习进度与内部模拟进度必须继续分离。
- 两本讲义已完成 562/562 页 OCR 与原页图云端接入；OCR 是检索辅助而非公式事实源，142 条课程页映射当前为 `candidate`，只有逐页视觉核验后才可升级为 `verified`。
- Cloudflare D1/R2 导入由 `cloud/mcp/scripts/import_content.mjs` 生成版本、哈希和可重复构建证据；后续内容变更必须生成新版本且保护现有学习事件。当前新计划已通过内存 SQLite 双次幂等执行和用户关联行保护。
- 全量 Python 回归当前为 174 项通过、1 项因本机缺少旧 DeepSeek 视觉配置跳过；Cloud MCP 为 8/8，通过增量迁移、完整内容/答案页导入、幂等重放与用户数据保护。
- `reports/final-acceptance.json` 仍不得用于声称真人完成。真实课程消费、最终数学作答、独立判分、真人观察和 24 小时冷复测继续保持独立 `not_run` 状态，不能用路线审计或代理预测替代。
