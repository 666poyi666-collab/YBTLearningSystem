# Known Issues

- 远程 Cloudflare MCP 已部署并导入选择性必修 1 五章全部资料；当前内容快照以最新 `source_versions` 记录为准，D1 应包含 5 章、38 节、1,209 个项目、170 门课程目录、19,234 个转写片段和 125 张题图。ChatGPT“数学一本通学习”已完成 OAuth 授权，并通过真实只读工具调用验收；网页本地状态不会自动产生学习事件，真实进度仍需用户确认后由写工具记录。
- 第二章 22 门课程已有完整文稿，但缺少可验证的逐句时间轴；恢复原视频/音频前不得伪造回看时间。
- 云端资料库已扩展至选择性必修 1 第一至第五章；第三至第五章学习路径尚未开始，不得把“资料可读”说成“学习路径完成”。
- 当前原书答案结构化来源为第一章 50 道 A/B/C 题；第二、三章答案 PDF 已存在于用户下载目录，但尚未完成答案 OCR/题号绑定，`math_get_answer_sources` 对这些题目会返回空来源。
- 现有真实学习进度与内部模拟进度必须继续分离。
- 两本讲义已完成 562/562 页 OCR 与原页图云端接入；OCR 是检索辅助而非公式事实源，142 条课程页映射当前为 `candidate`，只有逐页视觉核验后才可升级为 `verified`。
- Cloudflare D1/R2 导入已由 `cloud/mcp/scripts/import_content.mjs` 生成版本、哈希和可重复构建证据；后续内容变更必须生成新的版本号，不覆盖已有学习事件。
- 全量 Python 回归当前为 140 项通过、1 项因本机缺少 DeepSeek 视觉配置跳过。已修复旧 OCR 根路径解析、视觉侧车跨设备内容哈希绑定、首图奖励测试夹具、进度需求哈希、当前 1.1 模拟投影和第一章三节模拟汇总。
- 当前 `reports/final-acceptance.json` 仍保持 `IN_PROGRESS_NOT_PUBLISHABLE`，剩余真实门禁是桥接微单元的全部题目 release gate 和当前教师判卷 generation/hash；真人观察与 24 小时冷复测继续保持 `not_run`，不能用代理结果替代。
