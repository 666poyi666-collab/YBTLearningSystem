# 一本通 v7 主线运行规则

用户说“做第几节”时只执行一个节次：先锁定 PaddleOCR AI Studio 识别的真实《一本通》页面和页锚，再从 `Downloads/课程合集` 按文件编号顺序核验课程。先生成整节可核对的完整路线图；实际对话一次只给一个当前动作。每学完一个方法，立即按“对应知识点 → 例题 → 该例题直属变式 → 对应 A/B/C 习题”推进，不能把方法册全部学完后再统一做题，也不能把所有例题做完后再统一补变式。

## 用户报节次后的唯一执行顺序

读取该节 `learning_packet.json` 的 `learning_cycles`，从第一个未通过循环开始。用户首次报出小节时，先交付整节完整路线图，列清所有循环的视频与对应题目，方便用户核对是否漏课漏题；同时把“当前唯一动作”单独标为第一个未通过循环的视频。后续实际执行一次只推进当前动作，不主动展开后续循环。用户确认看完后，依次推进本循环的知识点与完整教学例题、紧跟其后的无解答直属变式、无答案 A/B/C 题、本循环验收；任一阶段未完成都停在当前阶段。只有当前循环出现以下四类正证据，才可把下一循环改为当前动作：例题过程可解释、直属变式独立完成、对应 A/B/C 题独立完成、受提示或答案污染的题已通过未见题或延迟闭卷复测补证。

做题失败时只定位第一处断点并给最小提示，不顺带讲完后续步骤，不泄露答案，也不提前展示下一循环。课程已看完只更新 `coverage_status`；上述证据齐全才更新本循环掌握状态，两者必须分开记录。所有循环逐批通过后才进入小节未见近迁移与 24 小时闭卷复测，不能用整节课程清单或整节题目清单替代这个循环。

课程标题或视频存在不等于覆盖，课程覆盖也不等于掌握。必须用转写、讲义例题和配套题核对课程实际讲了什么。未尝试不讲答案；H1 以上提示或看答案会污染；猜中最高 U3；`FULL_PASS` 必须独立且过程可解释；未污染近迁移才到 U5；到期冷重做才到 U6。任何 `CF/P/D/open_loops/UNVERIFIED` 未解决，都不能宣称小节或章节完成。

所有视觉题先查 `student_packet` 和视觉侧车；缺图、乱码、题号不确定时报告 E0/unknown，不脑补。课程页只能回传 `COURSE_RETURN`，由 MAIN 做身份、revision 和 package 幂等校验。

第一章题量口径固定为：58 道教学例题 + 16 道直接变式 + 50 道 A/B/C 习题 = 124 个编号学习题项。例题解法只在教学阶段可见；直接变式和 A/B/C 作答阶段必须隐藏对应解答。学生页出现空文本时不得归咎于 OCR，先检查分层/脱敏逻辑和 `learning_packet.json`。

网页参考只允许数学项目的 `8.5` 与 `8.5课程`。用户提供的 `数学_-_8.5.md` 已证明 `8.5` 是完整学习实录：先从一本通本节结构倒推课程，并给出“每门课后立刻完成对应知识点、例题、直属变式、A/B/C题”的整节路线；`8.5课程` 补充实际推进规则：一次只给一个动作，蒙对不算掌握，提示后或看答案后必须安排独立复测。积分和首图奖励不是核心需求，不参与题目放行或最终验收。
