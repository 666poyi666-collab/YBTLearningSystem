# 项目总览

## 当前接管范围（2026-08-22）

- 活动章节：第一章、第二章；合计 11 节、401 个 canonical 学习项目。
- 内部路线证据：现有 v2 分节交付与五人格压力测试保留。
- 主学习者：`primary-user-proxy`，章节内按教材顺序运行并跨节保留画像。
- 课程账本：第一章 15 门、第二章 22 门，当前均为 `not_started`，不得用历史代理结果冒充已学完。
- learner-facing 输出合同已改为紧凑两层结构；第一、二章 HTML/Markdown 已重新生成，复制给 ChatGPT 已改为 MCP 优先、GitHub 回退。
- 第三至第五章保留数据和历史交付，不在当前新交付范围。

需求权威入口：`docs/PRODUCT-REQUIREMENTS.md`。当前验收报告为 `reports/final-acceptance.json`，历史报告不覆盖当前状态。

- 状态：`blocked`
- 分类：`active-owned candidate`；核对：`2026-08-17`
- 对话：小工具 10 条 LUNA section + 学习 299 条；当前 1.1 投影代为 `G-LUNA-20260822-01`
- 总验收：`IN_PROGRESS_NOT_PUBLISHABLE`

## 这是什么
从受控教材/OCR/课程语料生成全书分节学习路径、无答案题包、视觉证据和零基础模拟的验收管线。

## 用户需求
- [x] `[passed]` 不缺题漏题，按知识点→右侧例题→直属变式→类型题→A/B/C 顺序。
- [x] `[passed]` 第一、二章 11 节路线均完成 5 轮 × 5 人格压力测试，答案严格隔离。
- [ ] `[in_review]` 只允许指定 `课程合集`、`8.5`、`8.5课程` 和当前 PaddleOCR 证据。

## AI 解释
- section worker PASS、代理模拟、真人观察和 24 小时冷复测是不同层次。

## 已满足需求
- [x] `[passed]` PaddleOCR fresh run 69/69 文档、220 图像；第一章题包 50/50 可消费。
- [x] `[passed]` 1.1 当前代 5 个 worker、38/38、5/5；其余第一章三节合计 86/86 项当前模拟通过。
- [x] `[passed]` 第一、二章静态审计 11/11 节、401/401 项、题图和课程转写完整。

## 未完成需求
- [ ] `[blocked]` 仍有桥接微单元未达到全部题目 release gate，不能把所有题目标记为可直接验收。
- [ ] `[blocked]` 当前教师判卷文件缺失，不能补写或伪造 generation/hash 证据。
- [ ] `[not_run]` 真人观察和 24 小时冷复测。

## Bug
- [x] `[fixed]` 旧设备 OCR 路径导致的 packet 视觉侧车无法复核：现在按当前 OCR 根目录和内容哈希解析。
- [x] `[fixed]` 进度账本需求哈希过期、旧章节模拟报告过期、HTML GitHub-only 提示词不一致。
- [ ] `[open]` 桥接课程 release gate 与当前教师判卷仍需真实证据闭合。

## 日志与证据
- 最终门禁：`reports/final-acceptance.json`
- section 输出：`reports/luna_sections`

## 启动与开发
- `python -m ybt_learning.cli build-chapter1`
- `python -m ybt_learning.cli simulate-five`
- `python -m ybt_learning.cli verify-packet --packet data/packets/1.1/student_packet.json`
- `python -m unittest discover -s tests -v`

## 长期维护
- 禁止老人版、8.5g、数学摄像头和相邻语料；答案侧车/答案 OCR 不进入学生上下文。
- worker 只写自己的 `reports/luna_sections/<task-id>`。

## 对话来源
- `C:\开发\学习` 299 条和小工具 LUNA-YBT 10 条均已按 inventory 读取。

